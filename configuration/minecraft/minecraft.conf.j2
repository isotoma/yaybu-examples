description "Minecraft instance running in {{ path }} as {{ user }}"

start on filesystem$
stop on runlevel [!2345]$

chdir {{ path }}
exec su -s /bin/sh -c 'exec "$0" "$@"' {{ user }} -- java -Xmx1024M -Xms1024M -jar {{ path }}/minecraft_server.jar nogui

